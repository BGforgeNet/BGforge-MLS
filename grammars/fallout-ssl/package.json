{
    "name": "tree-sitter-ssl",
    "version": "0.1.0",
    "description": "Fallout 2 Star-Trek Scripting Language grammar for tree-sitter",
    "keywords": [
        "fallout",
        "ssl",
        "tree-sitter"
    ],
    "license": "",
    "author": {
        "name": "BGforge",
        "email": "dev@bgforge.net",
        "url": "https://bgforge.net/"
    },
    "files": [
        "grammar.js",
        "tree-sitter.json",
        "src/**",
        "*.wasm",
        "queries/**"
    ],
    "type": "module",
    "scripts": {
        "build": "tree-sitter generate && tree-sitter build --wasm",
        "generate:types": "pnpm exec dts-tree-sitter . | sed 's/export const enum /export enum /' > src/tree-sitter.d.ts && node ../../scripts/split-syntax-type.mjs src/tree-sitter.d.ts src/syntax-type.ts && cp src/tree-sitter.d.ts src/syntax-type.ts ../../server/src/fallout-ssl/",
        "test": "../../scripts/test-grammar.sh fallout-ssl",
        "lint": "oxlint grammar.js",
        "playground": "tree-sitter playground"
    },
    "devDependencies": {
        "@asgerf/dts-tree-sitter": "^0.21.0"
    }
}
