<script lang="ts">
    // Stub: hex/raw-bytes pane (follow-up tier). Per-field offset/size/raw already reach the cells via
    // the "Show bytes" toggle; a synced full-buffer hex pane is designed in plan Phase 4 and implemented
    // later. Rendered only if a layout opts into a raw block, which none does yet.
</script>
<div class="layout-stub">Raw bytes pane - not yet implemented.</div>
