<script lang="ts">
    // Stub: hex/raw-bytes pane, a deliberately deferred feature. Per-field offset/size/raw already reach
    // the cells via the "Show bytes" toggle; a synced full-buffer hex pane is deferred until a format
    // layout needs one. Rendered only if a layout opts into a raw block, which none does yet.
</script>
<div class="layout-stub">Raw bytes pane - not yet implemented.</div>
